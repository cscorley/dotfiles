My dotfiles. Everything in this repo is probably useless to you. It serves as
a repo for me to keep my crap. 

To use:

    git clone git://github.com/cscorley/dotfiles.git ~/.dotfiles
    cd ~/.dotfiles
    make install
